<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/dist/output.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <title>Authorize login</title>
</head>
<body class="bg-background flex items-center justify-center">
    <section class="flex flex-col items-center justify-center gap-4 w-[320px] mt-10">
        <form class="flex flex-col items-center gap-5 bg-white p-4 w-full rounded-lg shadow-2xl" action="/auth/qr/authorize" method="post">
            <div class="flex flex-col items-center gap-2">
                <h1 class="text-lg font-semibold">Authorize login</h1>
                <p class="text-center text-gray-500 text-sm">Use mock credentials shown in <br> placeholder to authorize your web login</p>
            </div>
            <input type="hidden" name="nonce" value="<%= nonce %>">
            <input type="text" name="username" class="w-full px-3 py-2 border-b border-b-neutral-400 focus:outline-none focus:border-primary outline-primary" autocomplete="off" placeholder="Username - admin" />
            <input type="password" name="password" placeholder="Password - password" class="w-full px-3 py-2 border-b border-b-neutral-400 focus:outline-none focus:border-primary outline-primary" />
            <button type="submit" class="px-4 py-2 rounded-md bg-primary text-white w-full">Authorize</button>
        </form>
    </section>
</body>
</html>